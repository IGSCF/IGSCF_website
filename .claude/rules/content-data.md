# Content Data

Text content that may change frequently should NOT be hardcoded inside components.

Store it in `src/data/`:

- `testimonials.js`
- `events.js`
- `programs.js`

Components should import data and map through it.
